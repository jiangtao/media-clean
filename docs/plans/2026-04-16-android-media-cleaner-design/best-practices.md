# 最佳实践

1. 任何会影响用户真实照片和视频的操作都必须默认安全，永不默认永久删除。
2. 识别规则必须可解释，候选项需要给出触发原因，避免“黑盒判断”。
3. 纯业务规则与原生能力解耦，确保后续替换为更强的原生分析能力时不牵连 UI。
4. 所有阈值集中管理，避免散落在组件中。
5. 先写失败测试再补实现，优先覆盖评分边界与回收站状态转换。
6. 文案面向家庭成员，避免使用工程术语；在代码中保留工程上的准确命名。
7. 对权限拒绝、空结果、扫描中、删除失败都要有明确状态视图。
