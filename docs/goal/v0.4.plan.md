# app-cleaner v0.4 Goal-Mode 执行计划

**Summary**
- 本轮唯一目标源是 [docs/goal/v0.4.md](/Users/jt/places/personal/app-cleaner/docs/goal/v0.4.md:1)，设计参考以 `../cleaner-app/preview-frames/final-screens/light|dark` 和当前总览图为准，Android-first 与扫描恢复真值继续以 [design/recognition-scan-android-first/_index.md](/Users/jt/places/personal/app-cleaner/design/recognition-scan-android-first/_index.md:1) 为约束。
- 核心实现决策已经冻结：`01-04` 仍归 `Photos` 单 route 内的四个子态；`05` 继续是 `RecycleBin` tab；`06` 继续是 `Settings` tab；通知逻辑不改业务语义；v0.4 第一波不引入新的 RN 全局状态管理库，先拆边界。
- 第一优先级不是“换视觉”，而是把当前 [src/ui/screens/PhotoGridScreen.tsx](/Users/jt/places/personal/app-cleaner/src/ui/screens/PhotoGridScreen.tsx:1) 的扫描编排、结果工作台、详情流拆开，同时保持 Android resume/checkpoint/notification 行为不回归。

**CLI Goal Bootstrap**
- 推荐 goal objective：`Execute app-cleaner v0.4 Android-first UI refactor end-to-end: land team-mode docs, split PhotoGrid into controller + entry/workspace/detail boundaries, align Landing/RecycleBin/Settings with the v0.4 design states, preserve scan resume and notification behavior, and finish with static gates, device validation, and clarify-based UX copy review.`
- 推荐完成定义：设计态 `01/02/03/04/05/06` 全部落地；`PhotoGridScreen` 完成边界拆分；`Landing/RecycleBin/Settings` 接上统一 theme+i18n；`typecheck + full vitest + android lanes + maestro smoke + manual design signoff` 全过；`clarify` 文案验收完成。
- 推荐阻断条件：任何 runtime error、type error、扫描恢复回归、通知行为回归、主设备 lane 失败，都冻结后续 UI polish，先修 P0。

**Wave Plan**
1. `Wave 0 / 计划落盘与规格冻结`。Owner：`baozheng + gongsunce`。先创建 `docs/plans/2026-05-06-v0-4-ui-team-plan/`，落盘 `_index.md`、`team-mode-board.md`、`bdd-specs.md`、`acceptance-matrix.md`；把 `04` 明确写成 `Photos` 内工作台子态而不是新 route；把“不引入全局状态库”“通知逻辑不变”“手机单栏 first”写成执行默认值。完成定义：执行文档足够让 CLI goal mode 直接按包推进，不再二次设计。验证：Lead 自审，文档互链正确。
2. `Wave 1 / PhotoGrid 控制面抽离`。Owner：`zhanzhao`。从 `PhotoGridScreen` 抽出 `usePhotoGridSessionController`，收走 permission、hydrate、start/resume/cancel、Android native attach、checkpoint、snapshot 持久化；`PhotoGridScreen` 降级为组装层。写范围以 `src/ui/screens/photo-grid/**` 为主，只在必要处接 `src/features/scan/**` 与 `src/services/storage/**` 的编排层，不动 SQLite schema。完成定义：扫描与恢复主逻辑可脱离 screen JSX 阅读和测试。验证：`npm run typecheck -- --pretty false`；`npm run test -- --run src/ui/screens/__tests__/PhotoGridScreen.test.tsx`。
3. `Wave 2 / 01-02-03 入口卡与 Landing 产品壳`。Owner：`zhanglong`。实现 `PhotoGridEntryCard`，统一承接授权态、扫描态、结果摘要态；同步重构 `LandingScreen`，让首页产品壳和设计图的“扫描 -> 识别 -> 筛选 -> 清理 -> 报告”主叙事对齐，并把硬编码双语副本收口到 [src/i18n/app-copy.ts](/Users/jt/places/personal/app-cleaner/src/i18n/app-copy.ts:1)。完成定义：`Landing` 和 `Photos` 顶部状态卡在 light/dark、zh/en 下都来自统一 copy 真值，且不回退成大面积 hero 卡。验证：`npm run test -- --run src/ui/screens/__tests__/LandingScreen.test.tsx src/ui/screens/__tests__/PhotoGridScreen.test.tsx src/i18n/app-copy.test.ts`。
4. `Wave 3 / 03-04 结果工作台与详情流`。Owner：`zhaohu`。实现 `PhotoGridWorkspace` 与 `PhotoGridDetailFlow`，把 filter、grid、selectedIds、batch actions、preview flow 从主 screen 状态树拆开；`PhotoGrid` 保持 dumb component，不再内部二次决定筛选真值；详情流保留“单击进详情、长按进入选中、扫描中也能冻结快照看详情”的现有交互语义。完成定义：`04` 成为清晰的结果工作台子态，筛选/选中/详情逻辑互不缠绕。验证：`npm run test -- --run src/ui/components/__tests__/PhotoGrid.test.tsx src/ui/components/__tests__/SegmentedControl.test.tsx src/ui/components/__tests__/PhotoGridItem-selection.test.tsx src/ui/screens/__tests__/DetailScreen.test.tsx src/ui/screens/__tests__/PhotoGridScreen.test.tsx`。
5. `Wave 4 / 05 回收站、06 设置、Theme 与 i18n 归一`。Owner：`wangchao`。让 `RecycleBinScreen` 与 `SettingsScreen` 对齐 v0.4 视觉层级；把回收站清理报告、空态、设置页说明文案并入 `app-copy`；在 [src/theme/app-theme.ts](/Users/jt/places/personal/app-cleaner/src/theme/app-theme.ts:1) 补足 v0.4 需要的语义 token，但不改变 theme preference 存储协议。完成定义：回收站、设置页不再各自维护散落 copy；light/dark 视觉一致；设置页功能闭环不回归。验证：`npm run test -- --run src/ui/screens/__tests__/RecycleBinScreen.test.tsx src/ui/screens/__tests__/SettingsScreen.test.tsx src/theme/app-theme.test.ts src/i18n/app-copy.test.ts src/application/AppPreferencesContext.test.tsx`。
6. `Wave 5 / 适配与验证接线`。Owner：`mahan`。把 safe-area、notch、foldable 基础能力真正接入 Photos、RecycleBin、Settings；默认目标是异形屏可用和不遮挡，不默认承诺全新双栏 IA；同时补 `acceptance-matrix` 中的设备 lane、人工设计对照、特殊屏 spot-check 清单。完成定义：目标页在打孔/刘海/横屏/大屏下没有明显遮挡和误触；验证路径可重复执行。验证：`npm run test -- --run src/features/compatibility/__tests__/screen-adaptation.test.ts src/ui/components/FoldableLayout.test.tsx`。
7. `Wave 6 / 集成收口与八贤王终验`。Owner：`baozheng + baxianwang`。主线程整合各包，先清 runtime/build，再跑全量静态门禁和设备门禁；随后基于 `.impeccable.md` 的既有设计上下文执行一次 `clarify` 文案验收，只允许回流 copy、标签、说明文字和轻微信息架构修正，不加新功能。完成定义：主路径无 error，文案达到“看得清、判得准、删得安心”，设计图与真实 UI 一一对照通过。

**Team-Mode Ownership**
- `baozheng`：总控波次、冻结规格、决定是否继续推进、集成收口，不接局部实现细活。
- `gongsunce`：专管边界、真值源、状态模型、是否需要后续 scoped store；本波只做架构裁决，不主写业务 UI。
- `zhanzhao`：主路径控制器和高风险集成，优先守住扫描恢复和 notification 行为。
- `zhanglong`：Landing + Entry Card + 顶部产品叙事。
- `zhaohu`：Photos 结果工作台、筛选、选中、详情流。
- `wangchao`：RecycleBin + Settings + theme/i18n 收口。
- `mahan`：适配接线、验证矩阵、设备 lane 证据。
- `baxianwang`：终验，不接受“没证据的完成”。

**Verification And Acceptance**
- 静态硬门槛：`npm run typecheck -- --pretty false`、`npm run test -- --run`。
- 定向关键护栏：`LandingScreen`、`PhotoGridScreen`、`DetailScreen`、`RecycleBinScreen`、`SettingsScreen`、`scan-completion-notifications.test.ts`、`screen-adaptation.test.ts`。
- Android lane 顺序：先 `verify:android:acceptance` 或 `verify:android:permission-denied`，再 `verify:android:scan-complete`、`verify:android:continue-scan`、`verify:android:recycle`、`verify:android:recycle-delete`。
- 次级交互信号：`npm run test:maestro:smoke`，只作为 fallback smoke，不替代 agent-device。
- 人工设计对照：逐页核对 `01/02/03/04/05/06`，每页至少看 `light/dark` 与 `zh-CN/en-US`。
- 特殊屏抽检：至少 `1 台打孔/刘海` 与 `1 台横屏/大屏或折叠态`；若本波没有新双栏 IA，只验安全区、操作可达和布局稳定。
- 文案 clarity 终验：扫描说明、完成说明、回收站空态、提醒解释、语言/主题/扫描范围说明必须经过 `clarify` review。

**Assumptions**
- 本波默认“手机单栏 first”；折叠/平板以可用性和安全区正确为验收目标，除非执行中另行冻结新的大屏 IA。
- v0.4 第一波不引入 Redux/Zustand 等新的全局状态库；若 `Wave 1` 后仍发现跨页面并发订阅同一份扫描会话状态且 `controller` 难以收口，再单独评估 scoped store，而不是一开始全局化。
- `docs/goal/v0.4.md` 当前虽未入 Git，但仍按当前活跃目标源执行；执行过程中不得修改该文件。
- 若控制器抽离与设计还原发生冲突，优先守住 Android resume/checkpoint 正确性，再做第二波视觉精修，不允许带着恢复语义回归进入终验。
